# Web App (Next.js) — Phase 1

- Pages:
  - `/` simple feed
  - `/create` listing form
  - `/listings/[id]` listing detail with video hero

Use Supabase client for auth + DB. Use Mux for upload/playback.
